En el folder de pusher-go-realtime-graph se encuentra lo respectivo a la graficación.
En el folder sorting-algorithms se encuentra lo respectivo a los algoritmos de ordenamiento.

Dentro de sorting-algorithms se encuentran dos folders (Algorithms y Randomizer) en algorithms están los algoritmos de ordenamiento, 
cada uno con su propio archivo, y dentro de Randomizer está el archivo que contiene las funciones del generador de pseudo aleatorios

El código no es corrible desde estas mismas direcciones. Deben iniciarse los modulos desde el respectivo gopath para su ejecución.